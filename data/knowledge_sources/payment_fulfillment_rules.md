# 支付、退款与物流口径

支付成功使用 `支付记录.支付状态 = '支付成功'`，支付金额使用 `支付记录.支付金额`，手续费独立使用 `支付记录.手续费`。退款完成使用 `退款记录.退款状态 = '退款完成'`，金额使用 `退款金额`。

物流签收使用 `物流单.物流状态 = '已签收'`；履约时效可比较 `物流单.实际送达时间` 与 `物流单.预计送达时间`。详细轨迹使用 `物流节点.事件类型` 和 `物流节点.发生时间`。
