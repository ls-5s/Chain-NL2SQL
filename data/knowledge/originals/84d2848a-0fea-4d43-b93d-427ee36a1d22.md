# 订单状态规则

## 状态含义

- `paid`：订单已支付，可以纳入默认销售额统计。
- `pending`：订单尚未支付，默认不纳入已实现销售额。

## 统计约定

如果问题没有特别说明，“销售额”“已实现收入”默认只统计 `paid` 订单。如果问题明确要求订单总额或包含待支付订单，必须在回答或 SQL 中说明统计范围。

## 时间字段

订单时间使用 `orders.created_at`。按天、月或时间范围统计订单时，应使用这个字段，而不是用户注册时间或商品信息中的字段。
