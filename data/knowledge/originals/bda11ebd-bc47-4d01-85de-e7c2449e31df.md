# 销售、收入与毛利口径

公司制度中的销售额统计口径：销售额默认统计支付成功订单的实付金额，不包含待支付、支付失败或已取消订单。

GMV 为支付成功订单的 `商品金额` 汇总；实付收入为支付成功订单的 `实付金额` 汇总。优惠成本使用 `优惠金额`，运费收入或成本不能混入商品金额。

商品毛利按订单明细计算：`订单明细.成交单价 * 订单明细.数量 - 商品.成本价 * 订单明细.数量`。历史交易价格应使用 `订单明细.成交单价`，不能用当前 `商品.销售价` 替代。

退款率以 `退款记录.退款状态 = '退款完成'` 的退款金额除以同期实付收入计算。
