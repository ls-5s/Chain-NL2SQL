# 订单与履约状态规则

`订单.订单状态` 为 `已完成`、`待支付` 或 `已取消`；`支付状态` 为 `支付成功`、`待支付` 或 `支付失败`；`履约状态` 为 `已签收`、`运输中` 或 `待发货`。

未特别说明时，订单收入只统计 `订单.支付状态 = '支付成功'` 的 `订单.实付金额`。订单数使用 `COUNT(DISTINCT "订单"."编号")`，避免关联 `订单明细` 后重复计数。下单时间使用 `订单.下单时间`，不要使用用户创建时间。
